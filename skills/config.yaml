schema_version: "2.0"

# Compatibility for repositories that still read the old switches.
# Adoption is explicit; this file is not needed to use an individual skill.
# Missing configuration also means no automatic enforcement.
behavior_switches:
  provenance_story:
    enabled: false
    require_readme_section: false
    update_chronicle: false
  responsible_vibe_workflow:
    enabled: false
